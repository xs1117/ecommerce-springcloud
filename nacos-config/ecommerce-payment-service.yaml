spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_payment?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 736100
    driver-class-name: com.mysql.cj.jdbc.Driver
  rabbitmq:
    host: localhost
    port: 5672
    username: ecommerce
    password: ecommerce123

app:
  mq:
    enabled: true
  integration:
    order:
      service-id: ecommerce-order-service
      base-url: http://localhost:8087

management:
  endpoints:
    web:
      exposure:
        include: health,info



