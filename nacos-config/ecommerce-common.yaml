spring:
  jackson:
    time-zone: Asia/Shanghai

security:
  token-secret: "ecommerce-local-dev-secret-change-me"
  token-expire-minutes: 10080

management:
  endpoints:
    web:
      exposure:
        include: health,info

